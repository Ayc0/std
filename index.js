module.exports = {
  len: require('./len'),

  map: require('./map'),
  filter: require('./filter'),
  forEach: require('./forEach'),
  reduce: require('./reduce'),

  range: require('./range'),
  zip: require('./zip'),
};
